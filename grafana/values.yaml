fluent-bit:
  enabled: true
promtail:
  enabled: false
filebeat:
  enabled: false
grafana:
  enabled: true 
prometheus:
  enabled: true 

  alertmanager:
    persistentVolume:
      enabled: false

  server:
    persistentVolume:
      enabled: false